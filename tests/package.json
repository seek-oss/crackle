{
  "name": "@crackle-private/tests",
  "version": "0.0.3",
  "private": true,
  "scripts": {
    "sync:dependencies": "sync-dependencies --source crackle --from ../package.json && prettier --write package.json"
  },
  "dependencies": {
    "@crackle/core": "workspace:*",
    "@playwright/test": "^1.32.0",
    "fast-glob": "^3.2.12",
    "fixturez": "^1.1.0",
    "sync-dependencies": "^1.0.4"
  }
}
