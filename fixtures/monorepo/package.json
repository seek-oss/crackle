{
  "name": "crackle-fixtures-monorepo",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "nx run-many --target=dev --output-style=stream-without-prefixes",
    "build": "nx run-many --target=build --output-style=stream-without-prefixes",
    "package": "nx run-many --target=package --output-style=stream-without-prefixes",
    "start": "nx start web-app"
  },
  "dependencies": {
    "braid-design-system": "file:../../.yalc/braid-design-system"
  },
  "devDependencies": {
    "nx": "14.5.2"
  },
  "workspaces": [
    "site",
    "packages/*"
  ]
}
